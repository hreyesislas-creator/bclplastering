// Kept for compatibility — the dashboard now uses `LeadsList` for richer
// rendering. This file re-exports the new component under the old name.
export { LeadsList as LeadTable } from "./leads-list";
